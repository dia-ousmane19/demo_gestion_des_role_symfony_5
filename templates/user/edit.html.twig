{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container mt-4" style="width:400px">
		<h3 class="text-center">L'utilisateur</h3>
    <a href="{{ path('user_index') }}">
			<i class='fas fa-long-arrow-alt-left'></i>Retour a la liste</a>
    {{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <div>
    <div class="mt-2">
    {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
